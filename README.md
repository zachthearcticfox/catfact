# Catfact Generator
Make the module usable by running `BUILD.sh`

Requirements:
```bash
# python 3.8+
pip install requests
```

Example usage:
```python
>>> import catfact
>>> print(catfact.get_catfact())
'A Catfact'
```